import types from '../util/constants';

export const loginInit = () => ({
    type: types.login.loginInit,
});

export const loginRequest = () => ({
    type: types.login.loginInit,
});

export const loginSuccess = () => ({
    type: types.login.loginInit,
});

export const loginFailure = () => ({
    type: types.login.loginInit,
});